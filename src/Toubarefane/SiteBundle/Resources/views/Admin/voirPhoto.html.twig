{# src/Sdz/BlogBundle/Resources/views/Blog/voir.html.twig #}

{% extends "ToubarefaneSiteBundle::layoutAdmin.html.twig" %}

{% block title %}
  Lecture d'une image - {{ parent() }}
{% endblock %}

{% block toubarefanesite_body %}
<div class="col-md-8 col-xd-10">
                    <div class="panel">
                       
                        <div class="panel-heading">
                          {% if image is not null %}
                            <img class="img-responsive oimg" src="{{ asset(image.uploadDir ~ '/' ~ image.url) }}" alt="{{ image.alt }}" />
                            {% endif %}
                              <p>
                         <a href="{{ path('toubarefaneadmin_tousphoto') }}" type="button" class="btn btn-info">
                         <i class="icon-chevron-left"></i>
                              Retour à la liste
                         </a>
                           <a href="{{ path('toubarefaneadmin_supprimerphoto', {'id': image.id}) }}" type="button" class="btn btn-success">
                          <i class="icon-edit"></i>
                           Supprimer
                           </a>
                         
                          </p>
                        </div>
                    </div>
</div>
{% endblock %}
